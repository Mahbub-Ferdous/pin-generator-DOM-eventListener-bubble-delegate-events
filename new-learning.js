// how to transfer number to string by additio number and empty string : if we need to transfer a number to string than we just addditon a empty string with the number data type it will transform a purre string

// uses of isNaN()function: if we need to task with NaN(Not A Number) than we called isNaN() function like : const a = 'c'; if(isNan(a)){console.log(not a number)}else{console.log(number)}